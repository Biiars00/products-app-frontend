import { HeaderContainer, Title, SubTitle } from '../../styles/components/HeaderContainerStyles';

const Header = () => {
    return (
        <>
            <HeaderContainer>   
                <Title>Inventory JCT App</Title>
                <SubTitle>Admin</SubTitle>
            </HeaderContainer>
        </>
    )
}

export default Header;